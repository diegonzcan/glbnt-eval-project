# glbnt-eval-project
Globant Eval Project

The project aims to fulfill the following requirements:

- Create a mysql database with schema to ingest historical data
- Move historic data from files in CSV format to a mysql database local instance.
- Filter out and log incomplete records
- Backup table daily into Avro Files


